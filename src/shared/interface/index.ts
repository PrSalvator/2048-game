/**
 * Координаты ячейки на игровом поле
 * @property {number} row - Номер строки (начинается с 0)
 * @property {number} column - Номер колонки (начинается с 0)
 */
interface ICoordinates {
  row: number;
  column: number;
}

export type { ICoordinates };
